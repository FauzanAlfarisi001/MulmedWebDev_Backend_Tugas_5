# Tugas Mulmed Web Dev

## Instalasi

npm install

npm install express drizzle-orm pg bcrypt jsonwebtoken cors dotenv

npm install -D drizzle-kit nodemon

## Cara Menjalankan

docker compose up --build

docker exec -it express-perpustakaan sh

npx drizzle-kit push

exit

## Cara Menghentikan

docker compose down